<%= form_with(model: enrollment) do |form| %>
  <% if enrollment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(enrollment.errors.count, "error") %> prohibited this enrollment from being saved:</h2>

      <ul>
        <% enrollment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, 'Student' %>
    <%= form.collection_select(:user_id, @users, :id, :name) %>
  </div>
    <div>
    <%= form.label :teacher_id, 'Teacher' %>
    <%= form.collection_select(:teacher_id, @users, :id, :name) %>
  </div>
  <div>
    <%= form.label :program_id, 'Program' %>
    <%= form.collection_select(:program_id, @programs, :id, :name) %>
  </div>
  <div>
    <%= form.check_box :favorite %>
    <%= form.label :favorite, 'Favorite Teacher' %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
